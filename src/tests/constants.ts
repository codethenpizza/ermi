export const ROOT_CAT_ID = 1;

export enum ATTR_TYPE_ID {
    STRING = 1,
    NUMBER,
    DECIMAL
}

export enum PAYMENT_STRATEGY_ID {
    FullPrepayment = 1,
    TenPercPrepayment
}

export enum SHIPPING_TYPE_ID {
    LocalPickup = 1,
    Courier
}

export enum PICKUP_POINT_ID {
    DEFAULT = 1
}
